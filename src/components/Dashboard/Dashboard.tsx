import React from 'react';

class Dashboard extends React.Component{
    render() {
        console.log(this);
        return (
            <h2>Dashboard</h2>
        );
    }
}


export default Dashboard;